*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Arduino toolchain: downloaded/extracted locally, re-installed via `arduino-cli core install`
# (see arduino-cli.yaml). Not source; includes archives over GitHub's 100 MB file limit.
.tools/arduino15/
.tools/cli/
.tools/downloads/

# Nested upstream OpenBot checkout (separate repository with its own remotes)
/OpenBot/

# Kotlin/Gradle IDE session state
.kotlin/
